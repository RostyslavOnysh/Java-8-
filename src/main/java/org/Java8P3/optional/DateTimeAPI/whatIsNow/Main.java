package org.Java8P3.optional.DateTimeAPI.whatIsNow;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.ZonedDateTime;
import java.time.chrono.ChronoLocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.TimeZone;

public class Main {
    public static void main(String[] args) {
      /*
      // LocalDate


        LocalDate currentDate = LocalDate.now();
        System.out.println(currentDate);   // + читабельність
        // + plus days,years
        System.out.println(currentDate.plusDays(4));
        System.out.println(currentDate.plusYears(4));
        System.out.println(currentDate.format(DateTimeFormatter.ofPattern("dd-yyyy-MM")));

        LocalDateTime localDateTime = LocalDateTime.now();
        System.out.println(localDateTime);
        System.out.println(localDateTime.toLocalTime());


        ZonedDateTime zone = ZonedDateTime.now();
        System.out.println(zone);

        TimeZone timeZone = TimeZone.getDefault();
        System.out.println(timeZone.getDisplayName());

        System.out.println(timeZone.toZoneId());
       */
    }
}
